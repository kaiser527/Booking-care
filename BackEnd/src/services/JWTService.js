import db from "../models/index";

const getUserWithAllcode = () => {
  return new Promise((resolve, reject) => {
    try {
    } catch (e) {
      reject(e);
    }
  });
};

export { getUserWithAllcode };
